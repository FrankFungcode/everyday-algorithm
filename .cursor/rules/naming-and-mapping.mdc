---
description: DTO/VO/Param/Command 命名后缀强制 + Integration 层 Request/Response 后缀 + 层间映射策略（默认 builder，按需 MapStruct）
alwaysApply: true
---

# 命名与层间映射约束

> 完整决策与方案对比见 [ADR 0008](../../docs/decisions/0008-naming-and-mapping.md)。

## 命名后缀强制

**业务层（Controller / Application / Service）：**

| 后缀 | 用途 | 包路径 | 是否继承 `BaseDto` |
|---|---|---|---|
| `*Vo` | Controller 返回给客户端的视图对象 | `{module}/vo/` | 仅"代表实体回显"的继承 |
| `*Dto` | 跨层 / 跨模块数据传输对象，**不**直接作为 Controller 返回值 | `{module}/dto/` 或 `{module}/service/dto/` | 视实体投影需求而定 |
| `*Param` | Controller 入参（含 Bean Validation） | `{module}/param/` | 否 |
| `*Command` | Service 层入参（字段较多时） | `{module}/command/` | 否 |

**Integration 层（非 SDK 模式自定义 Feign 接口）：**

| 后缀 | 用途 | 包路径 | 是否继承 `BaseDto` |
|---|---|---|---|
| `*Request` | Feign 接口请求报文定义 | `{module}/integration/request/` | 否 |
| `*Response` | Feign 接口响应报文定义 | `{module}/integration/response/` | 否 |

**在业务层禁止使用 `*Result` / `*View` / `*Response` / `*Model` / `*POJO` 等后缀作为返回值后缀。`*Request` / `*Response` 仅限 Integration 层 Feign 接口数据结构，不得用于业务层。**

新建 Controller 返回值类必须用 `*Vo` 后缀；新建 service 间传递的 DTO 必须用 `*Dto` 后缀。AI 在生成代码时遇到老代码使用 `*Result` 命名时，**不要复制该风格**；保持新增类对齐本规则。

### 领域核心概念例外

已经具有清晰语义、跨模块复用的核心概念允许保留无后缀命名，但必须满足两个条件：

1. 在所属源码包内 `AGENTS.md` 中显式列入"领域核心概念例外清单"
2. javadoc 中标注"本类作为领域核心概念例外，不强制后缀"

未列出的不允许"自由命名"。新增例外需要 review 时讨论。

## 层间映射策略

### 默认：`.builder()` / 显式 setter

适用场景（覆盖 80%+ 实际用例）：

- 字段 ≤ 5 的 1:1 映射
- 仅在单个 Service / Application 中使用的转换
- 字段命名不一致或需要类型转换的场景
- 涉及嵌套对象、条件映射

```java
return TimesheetVo.builder()
        .id(entity.getId())
        .status(entity.getStatus())
        .build();
```

### 引入 MapStruct Converter 的场景

仅在以下场景使用 `@Mapper interface`：

- 列表批量映射（避免 `stream().map(...).toList()` 模板代码）
- 在 ≥ 2 个 Service / Application 中复用同一映射规则
- 跨多层链路（Entity ↔ DTO ↔ VO ↔ Param）需要集中管理映射规则
- `BaseMapper.toPagedDto` 等通用映射能力（继续保留）

新建 Converter 须继承 `BaseMapper`；自定义映射逻辑用 `@Named` / `@BeforeMapping` / `@AfterMapping`。

### 不允许

- 静态工具类风格的转换器（如 `XxxUtil.toVo`）
- Controller / Service 内重复出现"逐字段 set"的模板代码——应抽 builder 静态工厂或 Converter
- 简单 1:1 映射强行引入 MapStruct（违反 KISS）

## `BaseDto` 继承规则

| VO/DTO 类型 | 是否继承 `BaseDto` |
|---|---|
| 代表实体回显的 `*Vo`（含 `id` / `createdAt` / `createdBy` 等审计字段需要返回） | 继承 |
| 代表实体的 `*Dto`（跨层传递实体投影） | 继承 |
| 操作结果型 `*Vo`（"做了一件事的结果"，无实体语义） | **不继承** |
| 内部 service `*Dto`（领域操作结果） | **不继承** |

判断口径：**这个类的字段里是否需要 `id` / 审计字段语义**——需要就继承，不需要就不继承。

## AI 生成检查清单

在生成 Controller / Application / Service 代码前自检：

- [ ] 新建的返回值类是否用 `*Vo` 后缀（不是 `*Result`）
- [ ] 新建的 service 间 DTO 是否用 `*Dto` 后缀（不是 `*Result`）
- [ ] 新建的 Controller 入参是否用 `*Param` 后缀
- [ ] 是否被引诱去复制现有 `*Result` 风格——如果是，纠正回 `*Vo`/`*Dto`
- [ ] 字段 ≤ 5 的 1:1 映射是否在尝试引入 MapStruct——如果是，改回 `.builder()`
- [ ] 是否有"代表实体回显"的 VO 漏继承 `BaseDto`
- [ ] 是否有操作结果型 VO 错继承了 `BaseDto`

在生成 Integration 层 Feign 接口代码前自检：

- [ ] 非 SDK 模式定义 Feign 接口时，请求体是否用 `*Request` 后缀并放在 `integration/request/` 包
- [ ] 非 SDK 模式定义 Feign 接口时，响应体是否用 `*Response` 后缀并放在 `integration/response/` 包
- [ ] `*Request` / `*Response` 是否误用在了业务层（Application/Service）——如果是，改为 `*Param`/`*Command`/`*Dto`
